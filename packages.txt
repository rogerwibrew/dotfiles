# Core Hyprland Packages
# Note: Using official repo version for stability
# For latest features, consider: hyprland-git (AUR, compiles from source)
hyprland
xdg-desktop-portal-hyprland
qt5-wayland
qt6-wayland
hyprpaper

# Terminal
kitty

# Editor
neovim  # Requires version 0.10+ for LazyVim

# Network Management
iwd         # iNet wireless daemon (backend)
impala      # TUI for managing WiFi
avahi       # mDNS/DNS-SD for .local hostname resolution
nss-mdns    # NSS module for mDNS hostname resolution

# Development Tools
base-devel  # Includes gcc, g++, make, and other build tools
cmake
rust
eza
git
lazygit     # TUI for git operations
openssh
tmux
unison
uv  # Fast Python package manager
docker
lazydocker  # TUI for docker management

# Shell
zsh
zsh-completions
zsh-syntax-highlighting

# Application Launcher
wofi

# Status Bar
waybar

# Fonts
# Note: Must be installed before SDDM (SDDM requires ttf-font)
ttf-jetbrains-mono-nerd
ttf-font-awesome
noto-fonts
noto-fonts-emoji

# Display Manager
sddm
qt6-svg
qt6-declarative
qt6-5compat  # Qt5 compatibility module for SDDM theme effects

# Lock Screen & Idle Management
swaylock-effects
swayidle

# Notifications
mako
libnotify        # Provides notify-send command

# Screenshots
grim
slurp

# Clipboard
wl-clipboard
cliphist

# File Manager (terminal-based)
yazi
ffmpeg           # Video thumbnails in yazi
p7zip            # Archive extraction in yazi
jq               # JSON preview in yazi
poppler          # PDF preview in yazi (provides pdftoppm)
fd               # Fast file searching in yazi
ripgrep          # Content searching in yazi
fzf              # Fuzzy file navigation in yazi
zoxide           # Smart directory jumping in yazi
imagemagick      # Extended image format support in yazi

# PDF Viewer
zathura
zathura-pdf-mupdf

# Browser
chromium

# Scanner
simple-scan       # GNOME scanner application
sane              # Scanner backend library
sane-airscan      # Modern driverless scanner support

# Printing
cups              # OpenPrinting CUPS printing system
gutenprint        # High-quality generic printer drivers
system-config-printer  # CUPS printer configuration GUI

# Audio
pipewire
pipewire-pulse
pipewire-alsa
wireplumber
pamixer

# Brightness Control
brightnessctl

# System Utilities
polkit-kde-agent
xdg-utils
udisks2    # Disk management (provides udisksctl for mounting USB)
fastfetch  # System information tool
btop       # Resource monitor
curl       # HTTP client (needed for various install scripts)
unzip      # Archive extraction

# Theme & Appearance
qt5ct
nwg-look

# AI Tools
# claude-code (installed via curl script in bootstrap.sh)

# Package Manager
# yay (AUR helper - must be installed manually first)

# AUR Packages (install with yay)
# hyprpicker (color picker for Wayland)
# wlogout (logout menu)
# epson-inkjet-printer-escpr2 (Epson ET-2865 EcoTank driver)

# Notes:
# - All packages should be installed via yay (handles both official and AUR)
# - LazyVim will be cloned separately:
#   git clone https://github.com/LazyVim/starter ~/.config/nvim
# - nvm should be installed via the official script, not pacman
